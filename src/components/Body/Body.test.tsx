import { fireEvent, render } from "@testing-library/react";
import React from "react";

import { CountStore } from "../../stores/CountStore";

import { Body } from "./Body";

test("must add count on counter reset", () => {
  // TODO: Implement this
});
